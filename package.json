{
  "name": "marked-samples",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "coverage:serve": "npm run coverage && serve coverage"
  },
  "devDependencies": {
    "@vitest/coverage-c8": "^0.28.5",
    "@types/marked": "^4.0.8",
    "serve": "^14.2.0",
    "typescript": "^4.9.3",
    "vite": "^4.1.0",
    "vitest": "^0.28.5"
  },
  "dependencies": {
    "marked": "^4.2.12"
  }
}